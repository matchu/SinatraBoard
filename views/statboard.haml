!!!
%html
  %head
    %title Sinatraboard
    %link{:type => 'text/css', :rel => 'stylesheet', :href => '/reset.css'}
    %link{:type => 'text/css', :rel => 'stylesheet', :href => '/style.css'}
  %body
  #wrapper
    #data
      %ul
        - @panes.each do |pane|
          %li.pane
            %h2
              - if pane.link
                %a{:href => pane.link}= pane.title
              - else
                = pane.title
            = stats_for(pane)

